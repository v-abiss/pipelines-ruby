source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in pipelines-ruby.gemspec
gemspec

group :test do
  gem 'simplecov', '~> 0.12.0'
end
